# HPCTV 电视使用说明

## 简介

HPCTV 是一个基于 Android TV 开发的电视直播软件。

## 基本功能

- 进入应用后会先显示加载动画
- 视频默认全屏播放
- 切换频道时会显示加载遮罩和转圈提示
- 播放成功后，会短暂显示“正在播放：频道名”
- 按 `OK` 键可打开左侧频道列表
- 频道列表中按 `OK` 键可播放当前选中的频道

## 遥控器操作

- `上键`：切换到上一个频道
- `下键`：切换到下一个频道
- `OK`：打开频道列表，或在列表中确认播放
- `返回键`：
  - 频道列表打开时，关闭频道列表
  - 主播放界面时，按两下退出应用

## 频道列表说明

- 左侧显示当前节目单中的频道名称
- 使用遥控器 `上键 / 下键` 选择频道
- 当前焦点频道会显示蓝色背景


